[project]
name = "dippatyo"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "chess>=1.11.2",
    "pytest>=9.1.1",
    "torch>=2.9.1",
]

[tool.pytest.ini_options]
pythonpath = ["src"]
